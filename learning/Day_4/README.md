# 📚 День 4: Real-time Сообщения и Virtual Scrolling

## 🎯 Цель дня

Реализовать полноценный real-time функционал чата с WebSocket и кастомным Virtual Scrolling:

### ✅ Что реализовано:
- **WebSocket Gateway** - real-time коммуникации через Socket.io
- **Отправка сообщений** - мгновенная доставка с Optimistic UI
- **Real-time обновления** - списка чатов, счетчиков непрочитанных
- **Кастомный Virtual Scrolling** - без библиотек, полный контроль
- **Автоскролл и кнопка "Вниз"** - удобная навигация
- **Preview режим** - чат создается при первом сообщении
- **Персональные комнаты** - для системных уведомлений

## 📋 План изучения

### Часть 1: Backend WebSocket (3 часа)

1. **WebSocket Gateway** (1 час)
   - Архитектура Socket.io в NestJS
   - Жизненный цикл подключения
   - CORS и безопасность
   - JWT аутентификация в WebSocket

2. **Система комнат (Rooms)** (45 мин)
   - Персональные комнаты `user-${userId}`
   - Комнаты чатов `chat-${chatId}`
   - Автоматическое присоединение при подключении
   - Маршрутизация событий

3. **Обработка сообщений** (45 мин)
   - Событие `message:send`
   - Санитизация DOMPurify
   - Rate limiting (throttling)
   - Отправка в несколько комнат

4. **Real-time уведомления** (30 мин)
   - Событие `chat:created`
   - Обновление unreadCount
   - Интеграция с ChatsService

### Часть 2: Frontend Real-time (3 часа)

1. **Socket.io Client Plugin** (45 мин)
   - Инициализация в Nuxt plugin
   - JWT из cookie
   - Автоматическое переподключение
   - Присоединение к персональной комнате

2. **useSocket Composable** (45 мин)
   - Retry логика для слушателей
   - emitWithAck для подтверждений
   - Типизация событий
   - Автоочистка при unmount

3. **Real-time в компонентах** (1 час)
   - ChatList: обновление lastMessage и unreadCount
   - ChatList: добавление новых чатов
   - MessageList: новые сообщения
   - Optimistic UI паттерн

4. **Обработка состояний** (30 мин)
   - Pending/Success/Failed для сообщений
   - Временные ID и замена на реальные
   - Индикаторы загрузки

### Часть 3: Кастомный Virtual Scrolling (4 часа)

1. **Теория Virtual Scrolling** (1 час)
   - Проблемы обычного рендеринга
   - Концепция виртуализации
   - Архитектура: Container, Spacer, Viewport
   - Динамические высоты элементов

2. **useVirtualScroll Composable** (1.5 часа)
   - Расчет видимых элементов
   - Кеширование высот
   - Буферизация для плавности
   - Сохранение позиции при загрузке

3. **VirtualList Component** (1 час)
   - Интеграция с composable
   - Директива v-measure
   - Автозагрузка истории
   - Кнопка "Вниз" с badge

4. **Оптимизация производительности** (30 мин)
   - ResizeObserver для адаптивности
   - will-change для GPU ускорения
   - Debounce для скролла

## 📂 Структура файлов

### Backend
```
backend/src/
├── modules/
│   └── websocket/
│       ├── websocket.gateway.ts      # Основной Gateway
│       ├── websocket.module.ts       # Модуль WebSocket
│       └── guards/
│           └── ws-jwt.guard.ts       # JWT Guard для WS
└── common/
    └── utils/
        └── id.utils.ts                # Утилиты для ID
```

### Frontend
```
frontend/app/
├── plugins/
│   ├── socket.client.ts              # Socket.io инициализация
│   └── virtual-scroller.client.ts    # Virtual scrolling
├── composables/
│   └── useSocket.ts                  # Socket composable
├── components/
│   └── chat/
│       └── message/
│           └── List.vue               # Virtual scroll list
├── utils/
│   └── constants.ts                  # Конфигурация
└── types/
    └── vue-virtual-scroller.d.ts     # TypeScript типы
```

## 🔑 Ключевые концепции

### WebSocket vs HTTP

| HTTP | WebSocket |
|------|-----------|
| Request-Response | Двусторонняя связь |
| Stateless | Stateful |
| Новое соединение каждый раз | Постоянное соединение |
| Подходит для CRUD | Подходит для real-time |

### Проблема дублирования и решение

**Проблема:**
```typescript
// Отправляли всем в чате
this.server.to(`chat-${chatId}`).emit('message:new', message)
// И всем участникам (включая тех, кто в чате)
this.server.to(`user-${userId}`).emit('message:new', message)
```

**Решение:**
```typescript
// Проверяем, кто уже в чате
const socketsInChat = await this.server.in(`chat-${chatId}`).allSockets()
// Отправляем в персональные комнаты только тем, кто НЕ в чате
if (!isInChat) {
  this.server.to(`user-${userId}`).emit('message:new', message)
}
```

### Virtual Scrolling

**Без Virtual Scrolling:**
- 1000 сообщений = 1000 DOM элементов
- Высокое потребление памяти
- Лаги при скролле

**С Virtual Scrolling:**
- 1000 сообщений = ~30 DOM элементов
- Минимальное потребление памяти
- Плавный скролл 60 FPS

## ✅ Чек-лист

- [ ] WebSocket Gateway настроен
- [ ] JWT авторизация работает
- [ ] События чата реализованы
- [ ] Дублирование исправлено
- [ ] Socket.io client подключен
- [ ] useSocket composable создан
- [ ] Real-time обновления работают
- [ ] Virtual Scrolling внедрен
- [ ] Console.log удалены
- [ ] DRY утилиты созданы
- [ ] Axios заменен на $fetch
- [ ] Тесты написаны

## 🚀 Запуск и тестирование

```bash
# Запуск через Docker
docker compose up -d

# Проверка логов
docker compose logs -f backend
docker compose logs -f frontend

# Тестирование
node test-full-flow.js
node test-virtual-scroll.js
```

## 📚 Дополнительные материалы

- [OVERVIEW.md](./OVERVIEW.md) - Детальный обзор архитектуры
- [Backend_Implementation/](./Backend_Implementation/) - Backend реализация
- [Frontend_Implementation/](./Frontend_Implementation/) - Frontend реализация
- [QUICK_START.md](./QUICK_START.md) - Быстрый старт

---

**Результат:** Полноценный real-time чат с оптимизированной производительностью! 🎉
