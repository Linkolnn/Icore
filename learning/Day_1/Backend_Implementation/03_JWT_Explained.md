# 🔐 JWT Токены - Подробное объяснение

## Что такое JWT?

**JWT** (JSON Web Token) - это **токен для аутентификации**, который содержит закодированную информацию о пользователе.

**Простыми словами**: Это как **электронный пропуск** в здание. Показал пропуск охраннику → тебя пропустили.

---

## 🧩 Структура JWT токена

JWT состоит из **3 частей**, разделённых точкой:

```
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiI1MDdmMWY3N2JjZjg2Y2Q3OTk0MzkwMTEiLCJlbWFpbCI6ImpvaG5AZXhhbXBsZS5jb20iLCJpYXQiOjE3MzExOTQwMDAsImV4cCI6MTczMTc5ODgwMH0.SflKxwRJSMeKKF2QT4fwpMeJf36POk6yJV_adQssw5c

     Header          .         Payload          .       Signature
```

### 1️⃣ Header (Заголовок)

Содержит информацию о типе токена и алгоритме шифрования:

```json
{
  "alg": "HS256",   // Алгоритм: HMAC SHA-256
  "typ": "JWT"      // Тип: JSON Web Token
}
```

**Закодировано в Base64**: `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9`

### 2️⃣ Payload (Данные)

Содержит информацию о пользователе (claims):

```json
{
  "sub": "507f1f77bcf86cd799439011",  // Subject: ID пользователя
  "email": "john@example.com",        // Email пользователя
  "iat": 1731194000,                  // Issued At: когда создан (timestamp)
  "exp": 1731798800                   // Expiration: когда истечёт (timestamp)
}
```

**Закодировано в Base64**: `eyJzdWIiOi...`

**ВАЖНО**: Payload НЕ зашифрован! Любой может его декодировать. Не храните пароли!

### 3️⃣ Signature (Подпись)

Проверяет что токен не был изменён:

```
HMACSHA256(
  base64UrlEncode(header) + "." + base64UrlEncode(payload),
  secret  // Секретный ключ на сервере
)
```

**Результат**: `SflKxwRJSMeKKF2QT4fwpMeJf36POk6yJV_adQssw5c`

**Зачем**: Если кто-то изменит Payload → Подпись не совпадёт → Токен невалиден!

---

## 🔄 Как работает JWT в нашем приложении

### Регистрация/Вход:

```
1. Frontend → POST /auth/login { email, password }
2. Backend проверяет пароль
3. Backend создаёт JWT:
   const payload = { sub: user._id, email: user.email }
   const token = jwtService.sign(payload)  // Создаёт токен
4. Frontend ← Получает token
5. Frontend сохраняет token (localStorage)
```

### Защищённый запрос:

```
1. Frontend → GET /auth/profile
   Headers: Authorization: Bearer <token>
2. JwtAuthGuard → Извлекает токен из заголовка
3. JwtStrategy → Проверяет подпись токена
4. JwtStrategy → Проверяет срок действия (exp)
5. JwtStrategy → Ищет пользователя в БД по sub (userId)
6. Backend → req.user = user
7. Frontend ← Возвращает данные пользователя
```

---

## 🔐 Безопасность JWT

### ✅ Что защищено:

1. **Подпись токена**:
   - Невозможно подделать без секретного ключа
   - Если изменить Payload → Подпись не совпадёт

2. **Срок действия**:
   - Токен действителен только 7 дней (в нашем случае)
   - После истечения нужно войти заново

3. **Проверка пользователя**:
   - При каждом запросе проверяется существование пользователя в БД
   - Если пользователь удалён → токен невалиден

### ⚠️ Что НЕ защищено:

1. **Payload виден всем**:
   ```javascript
   // Любой может декодировать токен:
   const parts = token.split('.')
   const payload = JSON.parse(atob(parts[1]))
   console.log(payload) // { sub, email, iat, exp }
   ```
   **НЕ ХРАНИТЕ** в токене:
   - ❌ Пароли
   - ❌ Секретные данные
   - ❌ Номера карт

2. **Token Theft** (кража токена):
   - Если кто-то украл токен → может использовать
   - Защита: короткий срок действия, httpOnly cookies

3. **XSS атаки**:
   - JavaScript на странице может украсть токен из localStorage
   - Защита: httpOnly cookies, CSP headers

---

## 💾 Где хранить JWT токен?

### Вариант 1: localStorage (наш вариант для обучения)

```typescript
// Сохранить
localStorage.setItem('token', access_token)

// Получить
const token = localStorage.getItem('token')

// Использовать
fetch('/api', {
  headers: {
    'Authorization': `Bearer ${token}`
  }
})
```

**Плюсы**:
- ✅ Просто реализовать
- ✅ Работает везде
- ✅ Доступен из JavaScript

**Минусы**:
- ❌ Уязвим к XSS атакам
- ❌ Не защищён от malicious scripts

**Когда использовать**: Обучение, прототипы, trusted environment

### Вариант 2: httpOnly Cookies (production)

```typescript
// Backend устанавливает cookie:
res.cookie('auth_token', token, {
  httpOnly: true,     // Недоступен для JavaScript
  secure: true,       // Только HTTPS
  sameSite: 'strict', // Защита от CSRF
  maxAge: 7 * 24 * 60 * 60 * 1000, // 7 дней
})

// Frontend не имеет доступа
// Cookie автоматически отправляется с каждым запросом
```

**Плюсы**:
- ✅ Защита от XSS атак
- ✅ Автоматическая отправка с запросами
- ✅ Более безопасно

**Минусы**:
- ❌ Сложнее настроить CORS
- ❌ Уязвим к CSRF атакам (решается через sameSite)

**Когда использовать**: Production приложения

---

## 🕐 Срок действия токена

### В нашем приложении:

```typescript
// auth.module.ts
JwtModule.register({
  secret: process.env.JWT_SECRET,
  signOptions: {
    expiresIn: '7d',  // Токен действителен 7 дней
  },
})
```

### Возможные значения:

```typescript
expiresIn: '1h'    // 1 час
expiresIn: '30m'   // 30 минут
expiresIn: '7d'    // 7 дней
expiresIn: '30d'   // 30 дней
expiresIn: '365d'  // 1 год
```

### Как проверить истёк ли токен:

```typescript
// Декодировать токен
const payload = JSON.parse(atob(token.split('.')[1]))

// Проверить exp (expiration timestamp)
const now = Date.now() / 1000  // Текущее время в секундах
const isExpired = payload.exp < now

if (isExpired) {
  console.log('Токен истёк!')
  // Перенаправить на /login
}
```

---

## 🔄 Refresh Tokens (расширенная тема)

В production часто используют **два токена**:

1. **Access Token** (короткий срок: 15 минут):
   - Используется для запросов
   - Хранится в памяти приложения

2. **Refresh Token** (длинный срок: 7 дней):
   - Используется для обновления Access Token
   - Хранится в httpOnly cookie

**Процесс**:
```
1. Login → Получить access_token (15м) + refresh_token (7д)
2. Использовать access_token для запросов
3. Access token истёк (15м) → Отправить refresh_token
4. Backend проверяет refresh_token → Выдаёт новый access_token
5. Продолжить работу с новым access_token
```

**Зачем**:
- ✅ Короткий access token → меньше риск при краже
- ✅ Refresh token в httpOnly cookie → защита от XSS
- ✅ Можно отозвать refresh token (blacklist в БД)

**В нашем проекте**: Пока не реализовано (для простоты). Будет на День 2.

---

## 🧪 Тестирование токена

### Декодировать токен онлайн:

1. Зайдите на https://jwt.io/
2. Вставьте ваш токен
3. Увидите Header, Payload и проверку подписи

### Пример вашего токена:

```
Encoded (как выглядит):
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiI1MDdmMWY3N2JjZjg2Y2Q3OTk0MzkwMTEiLCJlbWFpbCI6ImpvaG5AZXhhbXBsZS5jb20iLCJpYXQiOjE3MzExOTQwMDAsImV4cCI6MTczMTc5ODgwMH0.SflKxwRJSMeKKF2QT4fwpMeJf36POk6yJV_adQssw5c

Decoded:
Header: { "alg": "HS256", "typ": "JWT" }
Payload: { 
  "sub": "507f1f77bcf86cd799439011",
  "email": "john@example.com",
  "iat": 1731194000,
  "exp": 1731798800
}
Signature: Verified ✓ (если ввести секретный ключ)
```

---

## 📝 Резюме

**JWT - это**:
- ✅ Токен для аутентификации (электронный пропуск)
- ✅ Состоит из: Header + Payload + Signature
- ✅ Подписан секретным ключом (невозможно подделать)
- ✅ Имеет срок действия (у нас 7 дней)
- ✅ Stateless (не нужна сессия на сервере)

**Как использовать**:
1. Получить токен при входе
2. Сохранить (localStorage или cookie)
3. Отправлять в заголовке: `Authorization: Bearer <token>`
4. Backend проверяет и возвращает данные

**Безопасность**:
- ✅ НЕ храните секреты в Payload (он виден всем)
- ✅ Используйте HTTPS
- ✅ Короткий срок действия
- ✅ httpOnly cookies для production

**Следующий шаг**: Изучите `02_API_Usage_Guide.md` - как интегрировать в Frontend!
