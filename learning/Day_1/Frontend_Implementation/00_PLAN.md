# 🎨 Frontend Auth - План реализации

## 🎯 Цель

Создать Login и Register страницы с интеграцией Backend API.

---

## 🎨 Стилистика (из макета)

### Дизайн система:
- **Цветовая схема**: Dark theme (темная тема)
- **Фон**: Темно-серый/черный
- **Элементы**: Закругленные углы (border-radius)
- **Инпуты**: 
  - Темный фон с легким контрастом
  - Placeholder: светло-серый (#808080)
  - Закругленные края (как в поисковике)
- **Кнопки**: Закругленные, контрастные
- **Минимализм**: Чистый, современный UI

### Шрифты:
- **Заголовки**: `5mal6Lampen.ttf` (дот-шрифт) ⭐
  - Путь: `frontend/app/assets/fonts/5mal6Lampen.ttf`
  - Использовать для: Login, Register, заголовки форм
- **Основной текст**: Sans-serif (системный)

### Референс:
- Поисковик: `layout(img)/components/шапка-поиск(в обычном chatlist).png`
  - Форма инпутов должна быть похожей
  - Темный фон, закругленные края
  - Placeholder серого цвета

---

## 📋 Что нужно создать

### 1. Настройка шрифтов
```
✅ Подключить 5mal6Lampen.ttf
✅ Создать CSS переменные для шрифтов
```

### 2. Pinia Store (Auth)
```
✅ useAuthStore
   - state: token, user, isAuthenticated
   - actions: register(), login(), logout(), fetchProfile()
   - localStorage для токена
```

### 3. API Service Layer
```
✅ services/api/auth.service.ts
   - registerUser()
   - loginUser()
   - getProfile()
   - Axios/Fetch wrapper
```

### 4. Composables
```
✅ composables/useAuth.ts
   - useAuth() composable
   - Обертка над store
```

### 5. Страницы
```
✅ pages/login.vue
   - Форма входа (email, password)
   - Валидация
   - Обработка ошибок
   - Редирект после входа

✅ pages/register.vue
   - Форма регистрации (name, email, password)
   - Валидация
   - Обработка ошибок
   - Редирект после регистрации
```

### 6. Компоненты
```
✅ components/auth/LoginForm.vue
   - Форма с инпутами
   - Стилизация (темная тема, закругленные углы)
   - Валидация

✅ components/auth/RegisterForm.vue
   - Форма с инпутами
   - Стилизация
   - Валидация

✅ components/ui/Button.vue (опционально)
   - Переиспользуемая кнопка

✅ components/ui/Input.vue (опционально)
   - Переиспользуемый инпут
```

### 7. Middleware
```
✅ middleware/auth.ts
   - Проверка авторизации
   - Редирект на /login если не авторизован
```

### 8. Стили
```
✅ assets/styles/variables.scss (если нужен)
   - CSS переменные (цвета, шрифты)
   
✅ assets/styles/auth.scss
   - Стили для Auth страниц
```

---

## 🏗️ Архитектура (Frontend Patterns)

### 1. **Composition API** (script setup)
```vue
<script setup lang="ts">
// Только Composition API, НЕ Options API
</script>
```

### 2. **Store Pattern** (Pinia)
```typescript
// Centralized state management
useAuthStore()
```

### 3. **Service Layer Pattern**
```typescript
// API вызовы изолированы в сервисах
authService.login()
```

### 4. **Component Composition**
```
LoginPage → LoginForm → Input + Button
```

### 5. **Props/Events Pattern**
```vue
// Parent → Child: props
// Child → Parent: emits
```

---

## 📝 Порядок реализации

### Шаг 1: Настройка шрифта (5 мин)
```
1. Подключить 5mal6Lampen.ttf в nuxt.config.ts или CSS
2. Создать CSS класс для заголовков
```

### Шаг 2: Pinia Store (15 мин)
```
1. Создать stores/auth.ts
2. Реализовать state, actions
3. Интеграция с localStorage
```

### Шаг 3: API Service (10 мин)
```
1. Создать services/api/auth.service.ts
2. Методы для register, login, profile
3. Обработка ошибок
```

### Шаг 4: Login страница (20 мин)
```
1. Создать pages/login.vue
2. Форма с email и password
3. Стилизация (темная тема, закругленные края)
4. Интеграция с API
5. Обработка ошибок
```

### Шаг 5: Register страница (20 мин)
```
1. Создать pages/register.vue
2. Форма с name, email, password
3. Стилизация
4. Интеграция с API
```

### Шаг 6: Middleware (5 мин)
```
1. Создать middleware/auth.ts
2. Проверка токена
3. Редирект на /login
```

### Шаг 7: Тестирование (10 мин)
```
1. Регистрация нового пользователя
2. Вход существующего пользователя
3. Проверка middleware (защищенные страницы)
```

**Итого**: ~85 минут

---

## 🎨 Стили (CSS переменные)

```scss
// Примерные переменные на основе макета
:root {
  // Colors
  --bg-primary: #1a1a1a;        // Основной фон
  --bg-secondary: #2d2d2d;      // Фон карточек/инпутов
  --text-primary: #ffffff;       // Основной текст
  --text-secondary: #808080;     // Placeholder, второстепенный текст
  --accent: #4a9eff;            // Акцентный цвет (кнопки)
  --error: #ff4444;             // Ошибки
  
  // Fonts
  --font-heading: '5mal6Lampen', monospace;  // Заголовки (дот-шрифт)
  --font-body: -apple-system, sans-serif;    // Основной текст
  
  // Border Radius
  --radius-sm: 8px;
  --radius-md: 12px;
  --radius-lg: 20px;            // Как в поисковике
  
  // Spacing
  --space-xs: 4px;
  --space-sm: 8px;
  --space-md: 16px;
  --space-lg: 24px;
  --space-xl: 32px;
}
```

---

## 📦 Структура файлов

```
frontend/
├── app/
│   ├── assets/
│   │   ├── fonts/
│   │   │   └── 5mal6Lampen.ttf           ✅ Уже есть
│   │   └── styles/
│   │       ├── variables.scss             ← Создать
│   │       └── auth.scss                  ← Создать
│   ├── components/
│   │   ├── auth/
│   │   │   ├── LoginForm.vue             ← Создать
│   │   │   └── RegisterForm.vue          ← Создать
│   │   └── ui/
│   │       ├── Button.vue                ← Опционально
│   │       └── Input.vue                 ← Опционально
│   ├── composables/
│   │   └── useAuth.ts                    ← Создать
│   ├── middleware/
│   │   └── auth.ts                       ← Создать
│   ├── pages/
│   │   ├── login.vue                     ← Создать
│   │   ├── register.vue                  ← Создать
│   │   └── index.vue                     ← Главная (защищённая)
│   ├── services/
│   │   └── api/
│   │       └── auth.service.ts           ← Создать
│   └── stores/
│       └── auth.ts                       ← Создать
└── nuxt.config.ts                        ← Обновить (шрифт)
```

---

## 🔒 Безопасность

- ✅ Токен в localStorage (для обучения)
- ✅ Валидация на клиенте
- ✅ Обработка ошибок API
- ✅ Защита routes через middleware
- ❌ httpOnly cookies (будет в Day 2)

---

## 🎯 Критерии готовности

- [ ] Шрифт 5mal6Lampen подключен для заголовков
- [ ] Login страница работает
- [ ] Register страница работает
- [ ] Токен сохраняется в localStorage
- [ ] После входа редирект на главную
- [ ] Middleware защищает routes
- [ ] Стили соответствуют макету (темная тема, закругленные углы)
- [ ] Обработка ошибок (неверный пароль, email занят)

---

## 📚 Best Practices

1. **Composition API** (script setup) - всегда
2. **TypeScript** - типизация всего
3. **Pinia Store** - централизованное состояние
4. **Service Layer** - изоляция API
5. **Component Composition** - переиспользуемые компоненты
6. **Validation** - на клиенте и сервере
7. **Error Handling** - понятные сообщения пользователю
8. **Responsive** - адаптивность

---

## 🚀 Начинаем!

**Следующий шаг**: Создаю Pinia Store и API Service!
