# 🎯 День 2: Frontend - Sidebar UI и Глобальный Поиск

> UI для ChatSidebar, AppHeader, MenuModal и глобального поиска пользователей

---

## ⚠️ ВАЖНО: НЕТ СИСТЕМЫ ДРУЗЕЙ!

**iCore Messenger работает как Telegram:**
- ❌ НЕТ Friend Request UI
- ❌ НЕТ запросов в друзья
- ❌ НЕТ списка друзей
- ✅ Глобальный поиск пользователей (через AppHeader)
- ✅ Можно писать любому пользователю напрямую

---

## 📚 Структура обучения

### 1️⃣ [Theory.md](./Theory.md) - Теория
Изучи концепции перед практикой:
- Адаптивный Layout (400px / 100vw)
- Component Composition Pattern
- Debounced Search (300ms)
- Search Results в Sidebar (не Dropdown!)
- Menu Dropdown под кнопкой (не Modal!)
- Семантический HTML5
- Строгие правила дизайна (тени, НЕТ границ, opacity hover)
- Store Pattern (Pinia Composition API)

**Время:** ~1-2 часа

---

### 2️⃣ [Practice.md](./Practice.md) - Практика
Пошаговая реализация:
- user.types.ts (User, SearchUsersParams, SearchUsersResponse)
- user.service.ts (searchUsers API функция)
- users.ts store (Pinia Composition API)
- BaseButton.vue (4 варианта: primary, secondary, ghost, icon)
- BaseInput.vue (с v-model pattern)
- AppHeader.vue (MenuButton + SearchInput)
- MenuModal.vue (dropdown под кнопкой)
- ChatSidebar.vue (интеграция всего)
- auth.scss (shared styles для auth страниц)
- Тестирование

**Время:** ~3-4 часа

---

### 3️⃣ [Checklist.md](./Checklist.md) - Чек-лист
Отслеживай прогресс по всем этапам

---

## 🎯 Цель дня

Создать Sidebar UI с:
- ✅ Адаптивный ChatSidebar (Desktop: 400px, Mobile: 100vw)
- ✅ AppHeader с MenuButton + SearchInput
- ✅ MenuModal dropdown под кнопкой (НЕ в центре экрана!)
- ✅ Глобальный поиск (name, userId, email)
- ✅ Debouncing 300ms
- ✅ Search Results отображаются **в Sidebar** (заменяют chat list)
- ✅ Chat-item дизайн по макету chat-component.png
- ✅ Семантические HTML5 теги (aside, header, nav, article)
- ✅ Строгие правила дизайна (единый фон, тени, НЕТ границ)

---

## 📦 Что получишь

### Layout Structure:

**Desktop (>859px):**
```
┌────────────────────┬──────────────────────────┐
│  SIDEBAR (400px)   │   CHAT WINDOW (flex: 1) │
├────────────────────┼──────────────────────────┤
│  ┌──────────────┐  │                          │
│  │ [☰] [ПОИСК🔍]│  │    "Выберите чат"        │
│  └──────────────┘  │                          │
│                    │      (placeholder)       │
│  (список чатов)    │                          │
│  или               │                          │
│  (результаты       │                          │
│   поиска)          │                          │
└────────────────────┴──────────────────────────┘
```

**Mobile (≤859px):**
```
Sidebar (100vw):
┌──────────────────────┐
│  ┌────────────────┐  │
│  │ [☰] [ПОИСК 🔍] │  │
│  └────────────────┘  │
│                      │
│  (список чатов)      │
│  или                 │
│  (результаты поиска) │
└──────────────────────┘
```

### Компоненты:

**1. ChatSidebar.vue (главный компонент)**
- Адаптивная ширина (400px / 100vw)
- Содержит AppHeader
- Условный рендеринг: Search Results ИЛИ Chat List
- Семантический тег: `<aside>`

**2. AppHeader.vue**
- MenuButton (открывает MenuModal)
- SearchInput с debounce 300ms
- v-model:searchQuery для двусторонней связи
- v-model:showResults для управления отображением
- Семантический тег: `<header>`

**3. MenuModal.vue**
- **НЕ в центре!** Dropdown под кнопкой (position: absolute)
- **НЕТ затемнения фона** (transparent overlay)
- BaseButton variant="icon" с x.svg иконкой
- Кнопки: Профиль, Настройки, Выйти
- Закрытие по Escape / клик вне

**4. BaseButton.vue**
- 4 варианта: primary, secondary, ghost, icon
- Hover через `opacity: 0.8`
- Loading state с анимацией
- Напрямую slot (БЕЗ span обёрток!)

**5. BaseInput.vue**
- v-model pattern с computed
- Slot для иконок (#icon)
- Error states
- Label support
- **НЕТ границ**, только `$shadow-input`

**6. Search Results**
- Отображаются **в ChatSidebar** (не dropdown!)
- Chat-item компоненты по макету chat-component.png
- Avatar + Name + UserID
- Hover через `opacity: 0.8`

---

## 🎨 Design System (СТРОГИЕ ПРАВИЛА!)

### Концепция: Объём через тени, НЕ через фоны!

### ✅ РАЗРЕШЕНО:
1. **Единый фон** - `background: $bg-primary` (#212121) для ВСЕХ элементов
2. **Объём через тени** - элементы выделяются ТОЛЬКО через `$shadow-block` и `$shadow-input`
3. **Единый радиус** - `border-radius: $radius` (28px) для ВСЕХ элементов
4. **НЕТ границ** - `border: none` (кроме focus состояний)
5. **Hover через opacity** - `opacity: 0.8`, НЕ изменение фона

### ❌ ЗАПРЕЩЕНО:
1. **НЕТ осветлений/затемнений фона** - `lighten()`, `darken()`, `$bg-tertiary` запрещены
2. **НЕТ границ** - `border: 1px solid ...` запрещён (кроме focus)
3. **НЕТ разных фонов** - все блоки имеют `$bg-primary`
4. **НЕТ rgba фонов при hover** - `background: rgba(255, 255, 255, 0.05)` запрещён

### Colors:
```scss
$bg-primary: #212121;      // ЕДИНСТВЕННЫЙ фон для ВСЕХ элементов
$text-primary: #FFFFFF;    // Основной текст
$text-secondary: #999999;  // Второстепенный текст
$text-placeholder: #555555; // Placeholder в input
$accent-primary: #FFC700;  // Желтый акцент
```

### Official Shadows (ТОЛЬКО ЭТИ ДВА):
```scss
// Для ВСЕХ блоков кроме input
$shadow-block: 0 0 4px 0 rgba(0, 0, 0, 0.05),
               inset 0 0 15px 0 rgba(255, 255, 255, 0.05);

// ТОЛЬКО для input элементов
$shadow-input: 0 0 4px 0 rgba(255, 255, 255, 0.05),
               inset 0 0 15px 0 rgba(0, 0, 0, 0.05);
```

**ВАЖНО:** НИКОГДА не создавайте свои тени!

### Font:
```scss
font-family: '5mal6Lampen', sans-serif;
```

---

## 🚀 Быстрый старт

1. Прочитай [Theory.md](./Theory.md)
2. Следуй [Practice.md](./Practice.md)
3. Отмечай [Checklist.md](./Checklist.md)

---

## 🎓 Что изучишь

### Layout & Design
- **Адаптивный Layout** - медиа-запросы через mixins (@include mobile)
- **Semantic HTML5** - aside, header, nav, article (НЕ div!)
- **Official Shadows** - правильное применение $shadow-block и $shadow-input
- **Строгие правила** - единый фон, объём через тени, НЕТ границ

### Component Patterns
- **Component Composition** - переиспользование BaseButton, BaseInput
- **v-model Pattern** - двунаправленная привязка данных
- **Props & Emits** - правильная передача данных
- **Conditional Rendering** - v-if для Search Results / Chat List
- **Slots Pattern** - #icon slot в BaseInput

### Performance
- **Debounced Search** - простая реализация с setTimeout (300ms)
- **Computed Properties** - кеширование для v-model
- **Conditional Display** - showResults для оптимизации рендера

### State Management
- **Pinia Composition API** - users.ts store
- **Actions & State** - searchUsers, searchLoading, searchResults
- **Composables** - useAuth, useRouter, useUsersStore

### Code Quality
- **DRY Principle** - auth.scss для shared styles
- **No Code Duplication** - переиспользование компонентов
- **Clean Code** - без лишних тегов, span, классов

---

## ⏱️ Время

| Активность | Время |
|-----------|-------|
| Теория | 1-2 ч |
| Практика | 3-4 ч |
| Тестирование | 30 мин |
| **Итого** | **~5-6 ч** |

---

## 💡 Советы

1. **Строго следуй правилам дизайна** - НЕТ границ, НЕТ разных фонов, только тени!
2. **Hover ТОЛЬКО через opacity** - никогда не меняй background на hover
3. **Используй mixins для адаптивности** - `@include mobile`, `@include tablet`
4. **Семантические теги обязательны** - aside, header, nav, article (НЕ div)
5. **Search Results в Sidebar** - НЕ dropdown, а замена chat list
6. **MenuModal под кнопкой** - НЕ в центре экрана, position: absolute
7. **DRY для auth стилей** - создай auth.scss для shared styles

---

## 🐛 Частые проблемы

### "Текст в BaseInput пропадает при вводе"
**Причина:** Использовался $bg-input вместо $bg-primary
**Решение:** `background: $bg-primary` + убедись что `color: $text-primary`

### "MenuModal в центре экрана"
**Причина:** Используется старый Modal Pattern с Teleport
**Решение:** Position absolute, top: 60px, left: 10px, БЕЗ Teleport

### "Есть затемнение фона при открытии меню"
**Причина:** Overlay с rgba background
**Решение:** `background: transparent` для overlay

### "Search Results как dropdown поверх списка"
**Причина:** Position absolute в AppHeader
**Решение:** Conditional rendering в ChatSidebar (v-if в chat-list)

### "Hover меняет background"
**Причина:** Использование `lighten()`, `darken()`, rgba background
**Решение:** `@include hover { opacity: 0.8; }`

### "Дублирование стилей в login.vue и register.vue"
**Причина:** Не создан shared файл
**Решение:** Создай auth.scss с общими стилями

---

## 🧪 Тестирование

### Проверь адаптивность:
1. Открой DevTools (F12)
2. Переключи в режим адаптивности (Ctrl+Shift+M)
3. Проверь Desktop (>859px): Sidebar 400px
4. Проверь Mobile (≤859px): Sidebar 100vw
5. Проверь что нет horizontal scroll

### Проверь поиск:
1. Введи в SearchInput текст (минимум 2 символа)
2. Подожди 300ms (debounce)
3. Результаты появляются **в Sidebar** (заменяют chat list)
4. Каждый результат имеет Avatar + Name + UserID
5. Hover работает через opacity
6. При очистке поиска возвращается chat list placeholder

### Проверь MenuModal:
1. Кликни на MenuButton (☰) в AppHeader
2. Dropdown появляется **под кнопкой** (не в центре!)
3. **НЕТ затемнения фона**
4. Есть кнопка закрытия (BaseButton icon с x.svg)
5. Нажми Escape - закрывается
6. Кликни вне - закрывается

### Проверь дизайн-систему:
1. Все элементы имеют `background: $bg-primary`
2. НЕТ границ (borders) нигде
3. Все hover используют `opacity: 0.8`
4. Используются ТОЛЬКО официальные тени
5. Радиус везде `$radius` (28px)

---

## ✅ Критерии завершения

Frontend считается завершенным когда:

### Layout ✓
- [ ] ChatSidebar адаптивный (400px / 100vw)
- [ ] AppHeader с MenuButton + SearchInput
- [ ] Mobile breakpoint (859px) работает
- [ ] Используется `@include mobile` mixin
- [ ] Семантические теги (aside, header, article) везде
- [ ] НЕТ лишних div, span обёрток

### Функциональность ✓
- [ ] MenuButton открывает MenuModal **под кнопкой**
- [ ] MenuModal **БЕЗ затемнения фона**
- [ ] MenuModal закрывается по Escape / клик вне
- [ ] SearchInput с debounce 300ms
- [ ] Search Results появляются **в Sidebar** (не dropdown!)
- [ ] Chat-item дизайн по макету chat-component.png
- [ ] Профиль / Настройки / Выйти работают

### Дизайн-система ✓
- [ ] Все элементы `background: $bg-primary`
- [ ] Используются ТОЛЬКО `$shadow-block` и `$shadow-input`
- [ ] НЕТ границ (borders) нигде (кроме focus)
- [ ] Hover ТОЛЬКО через `opacity: 0.8`
- [ ] НЕТ `lighten()`, `darken()`, rgba backgrounds
- [ ] Радиус везде `$radius` (28px)

### Store ✓
- [ ] users.ts store (Pinia Composition API)
- [ ] user.service.ts с searchUsers функцией
- [ ] user.types.ts с типами

### Code Quality ✓
- [ ] BaseButton БЕЗ лишних span обёрток
- [ ] auth.scss для shared styles
- [ ] НЕТ дублирования кода
- [ ] Checklist.md заполнен
- [ ] Нет консольных ошибок

---

## 🔗 Связь с другими частями

### Требуется завершить:
- **День 1: Frontend** - Auth pages, useAuth composable, auth store
- **День 2: Backend** - User Search API (GET /users/search)

### Следующие шаги:
- **День 3: Список чатов** - отображение чатов в Sidebar
- **День 4: Окно чата** - ChatWindow, MessageInput, Messages
- **День 5: WebSocket** - real-time обновления

---

**Удачи! 🚀**
