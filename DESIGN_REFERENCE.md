# 🎨 Справочник по Дизайну ИCore Messenger

## 📁 Расположение макетов
`/home/linkoln/Project/Icore/layout(img)/`

---

## 📄 Страницы

### Chatlist.png
**Назначение**: Главная страница со списком чатов  
**Путь**: `frontend/app/pages/chat/index.vue`

**Элементы:**
- Поиск вверху страницы
- 3 круглых кнопки под поиском (Stories/Статусы?)
- Список чатов с:
  - Аватар слева
  - Название чата
  - Ник отправителя + превью текста
  - Время справа
  - Желтый badge с цифрой (непрочитанные)

**Стиль:**
- Темный фон
- Серый текст для вторичной информации
- Закругленные элементы

---

### Chat.png
**Назначение**: Окно чата (пустой чат с правилами)  
**Путь**: `frontend/app/pages/chat/[id].vue`

**Элементы:**
- Хедер с:
  - Стрелка назад слева
  - Название чата по центру
  - "Кол. участников / время в сети" под названием
  - Иконка звонка справа
  - Иконка меню (три точки) справа
- Центральный блок с правилами чата
- Инпут внизу

**Правила (текст из макета):**
```
ЭТО ВАШ ЧАТ, ЗДЕСЬ МОЖНО
ОБМЕНИВАТЬСЯ БУКВАМИ С ВАШИМ
СОБЕСЕДНИКОМ.
ПРАВИЛА ВСЕХ ЧАТОВ:
1. НИКОМУ НЕ ГОВОРИТЬ ОБ ЭТОМ ЧАТЕ
   (ДАЖЕ СОБЕСЕДНИКУ);
2. НЕ МАТЕРИТЬСЯ. А ТО НЕ ДАНЕ
   РОСКОШНО;
3. НЕ СЛЕДОВАТЬ 1 ПРАВИЛУ.
```

---

### Chat-1.png
**Назначение**: Активный чат с сообщениями  
**Элементы:**
- Те же что и Chat.png
- Сообщения в центре
- Прокрутка

---

### Chanel chatlist.png
**Назначение**: Список каналов (похож на Chatlist)  
**Отличия:**
- Нет кнопок под поиском
- Специфичная иконка для каналов

---

## 🧩 Компоненты

### input block (в чате).png
**Назначение**: Форма ввода сообщения  
**Компонент**: `frontend/app/components/MessageInput.vue`

**Элементы:**
- Плейсхолдер "СООБЩЕНИЕ" слева
- 3 иконки справа:
  - Смайлик (эмодзи)
  - Скрепка (прикрепить файл)
  - Микрофон (голосовое)

**Стиль:**
- Темный фон
- Закругленные края
- Иконки серого цвета

---

### message.png
**Назначение**: Обычное текстовое сообщение  
**Компонент**: `frontend/app/components/Message.vue`

**Элементы:**
- Темный фон сообщения
- Закругленные края
- Текст белого цвета
- Время справа (серым)

**Пример**: "Привет, нормально" + "00:00"

---

### message-1.png - message-4.png
**Назначение**: Различные варианты сообщений
- Входящие/исходящие
- С вложениями
- Системные сообщения
- Группировка сообщений

---

### voice message.png
**Назначение**: Голосовое сообщение  
**Компонент**: `frontend/app/components/VoiceMessage.vue`

**Элементы:**
- Иконка микрофона
- Волновая форма (waveform)
- Длительность
- Кнопка play/pause

---

### чат.png
**Назначение**: Элемент списка чатов  
**Компонент**: `frontend/app/components/ChatItem.vue`

**Элементы:**
- Название чата слева
- Ник: текст сообщения под названием
- Время справа (00:00)
- Желтый badge с цифрой справа

**Размеры**: Компактный, ~60-80px высота

---

### шапка (в чате).png
**Назначение**: Хедер окна чата  
**Компонент**: `frontend/app/components/ChatHeader.vue`

**Элементы:**
- Стрелка назад
- Название чата + подзаголовок
- Иконки справа (звонок, меню)

---

### шапка-поиск(в канале).png / шапка-поиск(в обычном chatlist).png
**Назначение**: Хедер со строкой поиска  
**Компонент**: `frontend/app/components/SearchHeader.vue`

**Элементы:**
- Поле поиска с плейсхолдером "ПОИСК"
- Иконка поиска справа

---

### Список Папок.png
**Назначение**: Организация чатов в папки  
**Компонент**: `frontend/app/components/FolderList.vue`

**Функциональность**:
- Создание папок
- Drag & Drop чатов в папки
- Сворачивание/разворачивание

---

### Список каналов.png
**Назначение**: Специальный список для каналов  
**Компонент**: `frontend/app/components/ChannelList.vue`

**Отличия от обычных чатов**:
- Иконка канала
- Количество подписчиков
- Особая верстка

---

### текст блок (если чат только создан и он пустой).png
**Назначение**: Placeholder для пустого чата  
**Компонент**: `frontend/app/components/EmptyChatPlaceholder.vue`

**Содержимое**:
- Центрированный текст с правилами
- Мотивационный текст

---

## 🎨 Дизайн-система

### 🚨 СТРОГИЕ ПРАВИЛА ДИЗАЙНА (ОБЯЗАТЕЛЬНО К ИСПОЛНЕНИЮ)

**Концепция**: Объём через тени, НЕ через фон!

#### ✅ ЧТО РАЗРЕШЕНО:
1. **Единый фон** - `background: $bg-primary` (#212121) для ВСЕХ элементов
2. **Объём через тени** - элементы выделяются только через `$shadow-block` и `$shadow-input`
3. **Единый радиус** - `border-radius: $radius` для ВСЕХ элементов
4. **Никаких границ** - `border: none` (кроме focus состояний)
5. **Hover через opacity** - при наведении меняется прозрачность или тень, НЕ фон
6. **Универсальный spacing** - `padding: 10px` и `gap: 10px` для ВСЕХ элементов, блоков, компонентов и страниц

#### ❌ ЧТО ЗАПРЕЩЕНО:
1. **НЕТ осветлений/затемнений фона** - `$bg-tertiary`, `$bg-secondary`, `lighten()`, `darken()` запрещены при hover
2. **НЕТ границ** - `border: 1px solid ...` запрещён (кроме focus)
3. **НЕТ разных фонов** - все блоки имеют `$bg-primary`, разница только в тенях
4. **НЕТ rgba фонов при hover** - `background: rgba(255, 255, 255, 0.05)` запрещён

#### 💡 ПРАВИЛЬНЫЙ HOVER:
```scss
// ✅ Правильно - меняем opacity или тень
.element {
  background: $bg-primary;
  box-shadow: $shadow-block;
  @include transition;

  @include hover {
    opacity: 0.8; // Уменьшаем прозрачность
    // ИЛИ
    box-shadow: $shadow-block, 0 0 10px rgba(255, 255, 255, 0.1); // Усиливаем тень
  }
}

// ❌ Неправильно - меняем фон
.element {
  background: $bg-primary;

  @include hover {
    background: $bg-tertiary; // ЗАПРЕЩЕНО!
    background: lighten($bg-primary, 5%); // ЗАПРЕЩЕНО!
    background: rgba(255, 255, 255, 0.05); // ЗАПРЕЩЕНО!
  }
}
```

---

### Цветовая палитра (Официальная)

**Файл палитры**: `/home/linkoln/Project/Icore/layout(img)/цветовая палитра.png`

```scss
// === ОСНОВНЫЕ ЦВЕТА ИЗ ПАЛИТРЫ ===

// Темная тема (по умолчанию)
$color-dark: #212121;        // Темная (ТЕНЬ) - основной фон
$color-accent: #FFC700;      // Желтый акцент - badge, уведомления
$color-light: #FFFFFF;       // Светлая - текст, иконки

// === ПРОИЗВОДНЫЕ ЦВЕТА ===

// Фоны
$bg-primary: #212121;        // ✅ ЕДИНСТВЕННЫЙ фон для ВСЕХ элементов

// Текст
$text-primary: #FFFFFF;      // Основной текст (из палитры)
$text-secondary: #999999;    // Вторичный текст (ники, время)
$text-placeholder: #555555;  // Плейсхолдеры

// Акценты
$accent-primary: #FFC700;    // Основной акцент (из палитры)

// Состояния
$color-online: #4CAF50;      // Онлайн статус
$color-error: #F44336;       // Ошибки
$color-success: #4CAF50;     // Успех
```

### Использование цветов:

```scss
// ✅ Примеры правильного применения
.chat-item {
  background: $bg-primary;    // Единый фон
  color: $text-primary;       // #FFFFFF
  border-radius: $radius;     // Единый радиус
  box-shadow: $shadow-block;  // Объём через тень
  @include transition;

  @include hover {
    opacity: 0.8;             // ✅ Hover через opacity
  }
}

.badge {
  background: $accent-primary; // #FFC700
  color: $color-dark;          // #212121
  border-radius: $radius;      // Единый радиус
  box-shadow: $shadow-block;   // Объём через тень
}

input {
  background: $bg-primary;     // Единый фон
  color: $text-primary;        // #FFFFFF
  border: none;                // Без границ
  border-radius: $radius;      // Единый радиус
  box-shadow: $shadow-input;   // Тень для input

  &::placeholder {
    color: $text-placeholder;  // #555555
  }

  &:focus {
    box-shadow: $shadow-input, 0 0 0 2px $accent-primary; // Акцент при focus
  }
}
```

### Тени (Официальные)

**ВАЖНО: Только для темной темы!**

```scss
// === ОСНОВНАЯ ТЕНЬ ДЛЯ ВСЕХ БЛОКОВ (кроме input) ===
// Применяется к: карточкам, чатам, сообщениям, модалкам, dropdown и т.д.
$shadow-block: 0 0 4px 0 rgba(0, 0, 0, 0.05), 
               inset 0 0 15px 0 rgba(255, 255, 255, 0.05);

// === ТЕНЬ ДЛЯ INPUT ЭЛЕМЕНТОВ ===
// Применяется к: input, textarea, select
$shadow-input: 0 0 4px 0 rgba(255, 255, 255, 0.05), 
               inset 0 0 15px 0 rgba(0, 0, 0, 0.05);

// === ДОПОЛНИТЕЛЬНЫЕ ===
$shadow-focus: 0 0 0 3px rgba($color-accent, 0.3); // Focus состояние
$shadow-none: none; // Убрать тень
```

**Когда использовать:**

```scss
// ✅ Блоки мессенджера (чаты, сообщения, карточки)
.chat-item {
  box-shadow: $shadow-block;
}

.message {
  box-shadow: $shadow-block;
}

.card {
  box-shadow: $shadow-block;
}

// ✅ Input элементы
input, textarea, select {
  box-shadow: $shadow-input;
  
  &:focus {
    box-shadow: $shadow-input, $shadow-focus; // Комбинируем с focus
  }
}

// ❌ НЕ применять тени к:
// - Тексту
// - Иконкам
// - Инлайн элементам
// - background элементам
```

### Типографика

**Шрифты:**

```scss
// Кастомный пиксельный шрифт '5mal6Lampen' (ретро-стиль)
@font-face {
  font-family: '5mal6Lampen';
  src: url('@/assets/fonts/5mal6Lampen.ttf') format('truetype');
  font-weight: normal;
  font-style: normal;
  font-display: swap;
}

// Шрифт применяется ко ВСЕМ элементам
label,
input,
textarea,
select,
button,
h1, h2, h3, h4, h5, h6 {
  font-family: '5mal6Lampen', sans-serif;
}
```

**Правила для текста:**

```scss
// ✅ Заголовки (h1, h2, h3, h4, h5, h6) - ТОЛЬКО они в uppercase
h1, h2, h3, h4, h5, h6,
.title,
.heading {
  font-family: '5mal6Lampen', sans-serif; // Кастомный шрифт
  text-transform: uppercase;
  letter-spacing: 1px; // для h1: 2px
}

// Пример заголовка
.page-title {
  font-family: '5mal6Lampen', sans-serif;
  text-transform: uppercase;
  letter-spacing: 2px;
  @include font-styles(32px, 700, 1.2);
}

// ❌ Весь остальной текст - обычный (НЕ uppercase)
// Labels, paragraphs, buttons, links - обычный регистр
.label {
  // БЕЗ text-transform: uppercase
  @include font-styles(14px, 500, 1.4);
  color: $text-secondary;
}

// Placeholders - обычный текст
input::placeholder {
  // БЕЗ text-transform: uppercase
  font-size: 14px;
  color: $text-placeholder;
}

// Кнопки - обычный текст
.button {
  // БЕЗ text-transform: uppercase
  @include font-styles(16px, 500, 1.5);
}
```

### Mixins (из `mixins.scss`)

```scss
// === Адаптивные breakpoints ===
@mixin laptop { @media (max-width: 1919px) { @content; } }
@mixin tablet { @media (max-width: 1364px) { @content; } }
@mixin mobile { @media (max-width: 859px) { @content; } }

// === Стилевые ===
@mixin font-styles($font-size, $font-weight, $line-height) {
  font-size: $font-size;
  font-weight: $font-weight;
  line-height: $line-height;
}

@mixin transition {
  transition: all 0.35s;
}

@mixin hover {
  @media (hover: hover) and (pointer: fine) {
    @content;
  }
}

// Использование:
.button {
  @include font-styles(16px, 500, 1.5);
  @include transition;
  
  @include hover {
    transform: scale(1.05);
  }
  
  @include mobile {
    width: 100%;
  }
}
```

---

## 📐 Layout структура (Адаптивный)

### Desktop (≥1024px) - Двухколоночный Layout

```
┌──────────────────┬────────────────────────────┐
│   SIDEBAR        │     CHAT WINDOW            │
│   (300px)        │     (flex: 1)              │
├──────────────────┼────────────────────────────┤
│ [ПОИСК]     [🔍] │  [←] Название чата  [📞]  │
│                  │      Кол. участников       │
│ ( ) ( ) ( )      ├────────────────────────────┤
│                  │                            │
│ [👤] Чат 1  [9]  │   [Сообщения здесь]       │
│ [👤] Чат 2  [3]  │                            │
│ [👤] Чат 3       │                            │
│                  ├────────────────────────────┤
│                  │ [СООБЩЕНИЕ]  [😊][📎][🎤] │
└──────────────────┴────────────────────────────┘

Sidebar ВСЕГДА видимый           Chat Window показывает:
                                 - Выбранный чат
                                 - ИЛИ "Выберите чат" (если не выбран)
```

### Desktop - Нет выбранного чата

```
┌──────────────────┬────────────────────────────┐
│   SIDEBAR        │                            │
│                  │                            │
│ [ПОИСК]     [🔍] │                            │
│                  │        ┌─────────────┐     │
│ ( ) ( ) ( )      │        │             │     │
│                  │        │  Выберите   │     │
│ [👤] Чат 1  [9]  │        │     чат     │     │
│ [👤] Чат 2  [3]  │        │             │     │
│ [👤] Чат 3       │        └─────────────┘     │
│                  │                            │
│                  │   Placeholder с надписью   │
└──────────────────┴────────────────────────────┘
```

### Tablet/Mobile (<1024px) - Одноколоночный Layout

**Состояние 1: Список чатов (по умолчанию)**
```
┌──────────────────────────────┐
│  [ПОИСК]            [🔍]     │
├──────────────────────────────┤
│  ( )  ( )  ( )               │
├──────────────────────────────┤
│  [👤] Название чата    [9]   │ ← Клик
│       ник: текст             │
├──────────────────────────────┤
│  [👤] Название чата    [3]   │
│       ник: текст             │
└──────────────────────────────┘

Sidebar занимает 100% экрана
```

**Состояние 2: Открытый чат (после клика)**
```
┌──────────────────────────────┐
│  [←] Название чата    [📞][⋮]│ ← Кнопка назад
│      Кол. участников         │
├──────────────────────────────┤
│                              │
│   [Сообщения здесь]         │
│                              │
├──────────────────────────────┤
│  [СООБЩЕНИЕ]   [😊][📎][🎤] │
└──────────────────────────────┘

Chat занимает 100% экрана
Sidebar скрыт (transform: translateX(-100%))
Анимация плавного появления
```

---

## 🔄 Анимации и Transitions

### Рекомендуемые transitions:

```scss
// ✅ Hover эффекты - меняем opacity, НЕ фон
.chat-item {
  background: $bg-primary;    // Единый фон
  border-radius: $radius;     // Единый радиус
  box-shadow: $shadow-block;  // Объём через тень
  @include transition;

  @include hover {
    opacity: 0.8;             // ✅ Меняем opacity
  }
}

// Появление сообщений
.message-enter-active {
  animation: slideUp 0.3s ease-out;
}

@keyframes slideUp {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

// Badge pulse
.badge {
  animation: pulse 2s infinite;
}

@keyframes pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.7; }
}

// === АНИМАЦИЯ SIDEBAR (Mobile/Tablet) ===
// Плавное появление чата на мобильных
.chat-window {
  transition: transform 0.3s ease-in-out;
  
  // Скрыт (когда показываем sidebar)
  &.hidden {
    transform: translateX(-100%);
  }
  
  // Показан (когда открыли чат)
  &.visible {
    transform: translateX(0);
  }
}

// Альтернатива: использовать Vue transitions
.slide-enter-active,
.slide-leave-active {
  transition: transform 0.3s ease-in-out;
}

.slide-enter-from {
  transform: translateX(100%);
}

.slide-leave-to {
  transform: translateX(-100%);
}
```

---

## 📱 Responsive Design

### Breakpoints (Mixins):

```scss
@mixin laptop {
  @media (max-width: 1919px) {
    @content;
  }
}

@mixin tablet {
  @media (max-width: 1364px) {
    @content;
  }
}

@mixin mobile {
  @media (max-width: 859px) {
    @content;
  }
}

// Использование:
.element {
  width: 100%;
  
  @include laptop {
    width: 80%;
  }
  
  @include tablet {
    width: 90%;
  }
  
  @include mobile {
    width: 100%;
  }
}
```

### Desktop Layout (≥1024px)

```scss
.app-layout {
  display: grid;
  grid-template-columns: 300px 1fr; // Sidebar + Chat
  height: 100vh;
  
  .sidebar {
    // Всегда видимый
    grid-column: 1;
    overflow-y: auto;
  }
  
  .chat-window {
    // Занимает оставшееся пространство
    grid-column: 2;
    overflow: hidden;
  }
  
  // Placeholder когда чат не выбран
  .empty-state {
    display: flex;
    align-items: center;
    justify-content: center;
    height: 100%;
    
    .placeholder {
      text-align: center;
      color: $text-secondary;
    }
  }
}
```

### Tablet/Mobile Layout (используем mixins)

```scss
.app-layout {
  @include tablet {
    // Переключаемся на одноколоночный layout
    display: block;
    position: relative;
    overflow: hidden;
    
    .sidebar {
      // Sidebar по умолчанию видимый
      position: absolute;
      width: 100%;
      height: 100%;
      left: 0;
      top: 0;
      z-index: 1;
      @include transition; // transition: all 0.35s
      
      // Скрываем когда чат открыт
      &.hidden {
        transform: translateX(-100%);
      }
    }
    
    .chat-window {
      // Chat по умолчанию скрыт
      position: absolute;
      width: 100%;
      height: 100%;
      left: 0;
      top: 0;
      z-index: 2;
      transform: translateX(100%);
      @include transition; // transition: all 0.35s
      
      // Показываем когда выбрали чат
      &.visible {
        transform: translateX(0);
      }
    }
    
    // Кнопка "назад" только на мобильных
    .back-button {
      display: flex;
    }
  }
}
```

### Пример реализации (Vue):

```vue
<template>
  <div class="app-layout">
    <!-- Sidebar (список чатов) -->
    <div 
      class="sidebar" 
      :class="{ hidden: isMobile && selectedChatId }"
    >
      <ChatList @select-chat="handleSelectChat" />
    </div>
    
    <!-- Chat Window -->
    <div 
      class="chat-window"
      :class="{ visible: isMobile && selectedChatId }"
    >
      <!-- Placeholder когда чат не выбран -->
      <div v-if="!selectedChatId" class="empty-state">
        <div class="placeholder">
          <h3>Выберите чат</h3>
          <p>Выберите чат из списка, чтобы начать общение</p>
        </div>
      </div>
      
      <!-- Открытый чат -->
      <div v-else>
        <!-- Кнопка назад (только mobile) -->
        <button 
          v-if="isMobile" 
          class="back-button"
          @click="handleBack"
        >
          ← Назад
        </button>
        
        <ChatWindow :chat-id="selectedChatId" />
      </div>
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref, computed, onMounted, onUnmounted } from 'vue'

const selectedChatId = ref<string | null>(null)
const windowWidth = ref(window.innerWidth)

// Определяем мобильную версию
const isMobile = computed(() => windowWidth.value < 1024)

// Обработчик выбора чата
const handleSelectChat = (chatId: string) => {
  selectedChatId.value = chatId
}

// Обработчик кнопки "назад"
const handleBack = () => {
  selectedChatId.value = null
}

// Отслеживаем изменение размера окна
const handleResize = () => {
  windowWidth.value = window.innerWidth
}

onMounted(() => {
  window.addEventListener('resize', handleResize)
})

onUnmounted(() => {
  window.removeEventListener('resize', handleResize)
})
</script>
```

---

## 💡 Примеры компонентов с тенями

### ChatItem компонент:
```vue
<template>
  <div class="chat-item">
    <div class="avatar"><!-- ... --></div>
    <div class="info">
      <h4>Название чата</h4>
      <p>ник: текст сообщения</p>
    </div>
    <span class="badge">5</span>
  </div>
</template>

<style lang="scss" scoped>
.chat-item {
  background: $bg-primary;   // ✅ Единый фон
  border-radius: $radius;    // ✅ Единый радиус
  padding: $space-md;
  box-shadow: $shadow-block; // ✅ Объём через тень
  @include transition;

  @include hover {
    opacity: 0.8;            // ✅ Hover через opacity
  }
}

.badge {
  background: $accent-primary;
  color: $color-dark;
  border-radius: $radius;    // ✅ Единый радиус
  box-shadow: $shadow-block; // ✅ Объём через тень
}
</style>
```

### Message компонент:
```vue
<template>
  <div class="message" :class="{ outgoing }">
    <p>{{ text }}</p>
    <span class="time">{{ time }}</span>
  </div>
</template>

<style lang="scss" scoped>
.message {
  background: $bg-primary;   // ✅ Единый фон
  border-radius: $radius;    // ✅ Единый радиус
  padding: $space-sm $space-md;
  box-shadow: $shadow-block; // ✅ Объём через тень

  // Исходящие сообщения - тоже единый фон
  &.outgoing {
    background: $bg-primary;  // ✅ Единый фон
  }
}
</style>
```

### Input компонент:
```vue
<template>
  <div class="input-wrapper">
    <input
      v-model="message"
      placeholder="СООБЩЕНИЕ"
    />
    <button class="btn-emoji">😊</button>
    <button class="btn-attach">📎</button>
  </div>
</template>

<style lang="scss" scoped>
input {
  background: $bg-primary;   // ✅ Единый фон
  border: none;              // ✅ Без границ
  border-radius: $radius;    // ✅ Единый радиус
  padding: $space-sm $space-md;
  box-shadow: $shadow-input; // ✅ Тень для input

  &::placeholder {
    color: $text-placeholder;
  }

  &:focus {
    // Акцент при focus - разрешён
    box-shadow: $shadow-input, 0 0 0 2px $accent-primary;
  }
}

.btn-emoji,
.btn-attach {
  background: $bg-primary;   // ✅ Единый фон
  border: none;              // ✅ Без границ
  border-radius: $radius;    // ✅ Единый радиус
  box-shadow: $shadow-block; // ✅ Объём через тень
  @include transition;

  @include hover {
    opacity: 0.8;            // ✅ Hover через opacity
  }
}
</style>
```

---

## ✅ Чеклист реализации дизайна

### День 7:
- [ ] **Layout основной**
  - [ ] Desktop: Grid двухколоночный (300px + 1fr)
  - [ ] Mobile: Absolute positioning с анимациями
  - [ ] Responsive breakpoint на 1024px
- [ ] **Компоненты**
  - [ ] ChatList компонент + тени
  - [ ] ChatItem компонент + тени
  - [ ] Message компонент + тени
  - [ ] MessageInput компонент + тени input
  - [ ] ChatHeader компонент + тени
  - [ ] EmptyState компонент ("Выберите чат")
- [ ] **Логика состояний**
  - [ ] selectedChatId state
  - [ ] isMobile computed
  - [ ] handleResize listener

### День 10:
- [ ] **Дополнительные компоненты**
  - [ ] SearchHeader с фильтрами + тени
  - [ ] VoiceMessage компонент + тени
  - [ ] Badge компонент + тени
  - [ ] BackButton (только mobile)
- [ ] **Анимации**
  - [ ] Slide transition для mobile
  - [ ] Hover эффекты
  - [ ] Message появление (slideUp)
  - [ ] Badge pulse
- [ ] **Финальная полировка**
  - [ ] Проверка теней на всех компонентах
  - [ ] Тестирование на разных экранах
  - [ ] Smooth transitions
  - [ ] FolderList (если время) + тени
  - [ ] ChannelList (если время) + тени

---

**Используйте этот файл как reference при верстке компонентов!**
