// ===================================
// 🔐 AUTH PLUGIN - Восстановление сессии при загрузке
// ===================================
// .client.ts - выполняется только на клиенте
// Восстанавливает сессию из cookie перед рендерингом

export default defineNuxtPlugin(async (nuxtApp) => {
  const authStore = useAuthStore()
  
  try {
    // Восстанавливаем сессию при загрузке приложения
    await authStore.restoreSession()
  } catch (error) {
    // Молча обрабатываем ошибку - токен может быть просроченным
  }
})
